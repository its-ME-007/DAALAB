# ============================================================================
# DAALAB Scheduler-Worker Architecture Dependencies
# ============================================================================

# Core Web Framework
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0

# Visualization
matplotlib==3.8.2
plotly
streamlit

# Authentication & JWT (ES256 support)
PyJWT[crypto]>=2.8.0

# LangChain & AI Agent Dependencies
langchain>=0.1.0
langchain-groq>=0.1.0
langchain-core>=0.1.0
langchain-community>=0.0.20
langchain-classic  # For legacy AgentExecutor support

# HTTP client for microservice communication
httpx>=0.24.0,<0.28.0  # groq SDK passes 'proxies' which was removed in httpx 0.28
requests

# Code execution dependencies
docker>=6.0.0
python-dotenv
supabase

# AI services
mistralai

# Scheduler-Worker Architecture (New)
psutil>=5.9.0  # Resource monitoring for workers

# Static complexity analysis (C++ backend; Python uses stdlib ast)
tree-sitter>=0.22.0
tree-sitter-cpp>=0.22.0

# Testing & Load Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
locust>=2.15.0

# Development utilities
python-multipart  # For file uploads
